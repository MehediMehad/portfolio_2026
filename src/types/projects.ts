export interface TProject {
    id: string
    image: string
    title: string
    overview: string
    description: string
    techStack: string[]
    liveURL: string
    gitHubURL: string
    createdAt: string
}
