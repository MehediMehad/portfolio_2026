import { TMeta } from "@/types";

// reusable fallback meta
export const defaultMeta: TMeta = {
    page: 0,
    limit: 0,
    total: 0,
    totalPage: 0,
    hasNextPage: false,
    hasPrevPage: false,
};